"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mergeMetrics = exports.registerOnMetric = exports.initMetric = void 0;
const initMetric = () => ({ total: 0, max: 0, count: 0 });
exports.initMetric = initMetric;
const registerOnMetric = (total, metric) => {
    metric.total += total;
    metric.max = Math.max(metric.max || 0, total);
    metric.count = (metric.count || 0) + 1;
};
exports.registerOnMetric = registerOnMetric;
const mergeMetrics = (inMetric, outMetric) => {
    outMetric.total += inMetric.total;
    outMetric.max = Math.max(inMetric.max || 0, outMetric.max || 0);
    outMetric.count = (inMetric.count || 0) + (outMetric.count || 0);
};
exports.mergeMetrics = mergeMetrics;
//# sourceMappingURL=common.js.map